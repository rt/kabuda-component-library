import chai from 'chai';
import { setup } from './';

const expect = chai.expect;

describe('reference', () => {
    let model;

    describe('#setup', () => {
        it('should deserialize properties', () => {
            // setup(null, null, null);
        });
    });
});
